<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleName</key>
	<string>crossover-wheel</string>
	<key>CFBundleDisplayName</key>
	<string>crossover-wheel</string>
	<key>CFBundleIdentifier</key>
	<string>it.allard.crossover-wheel</string>
	<key>CFBundleExecutable</key>
	<string>t150menu</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleShortVersionString</key>
	<string>@VERSION@</string>
	<key>CFBundleVersion</key>
	<string>@VERSION@</string>
	<key>CFBundleIconFile</key>
	<string>AppIcon</string>
	<key>NSPrincipalClass</key>
	<string>NSApplication</string>
	<key>NSHighResolutionCapable</key>
	<true/>
	<!--
	    A menu bar item, not a windowed application: no Dock icon and no
	    entry in the application switcher. The setup window is still shown
	    on demand, and on first run.
	-->
	<key>LSUIElement</key>
	<true/>
	<!--
	    Twelve rather than eleven, because bootswitch.c and probe_intr.c
	    name kIOMainPortDefault, which IOKit renamed from
	    kIOMasterPortDefault in macOS 12 and marks available from there.
	    It is a data symbol, so dyld binds it when the process starts:
	    saying 11 here let LaunchServices open the application on a system
	    where it could only die before main, with a crash report and no
	    menu bar item to explain it. t150d links the same file, so falling
	    back to the command line would not have helped either.

	    APP_CFLAGS carries the matching -mmacosx-version-min so the
	    compiler refuses an API newer than this rather than leaving it to
	    be found by whoever runs it.
	-->
	<key>LSMinimumSystemVersion</key>
	<string>12.0</string>
</dict>
</plist>
