.Dd July 31, 2026
.Dt CROSSOVER-WHEEL 7
.Os
.Sh NAME
.Nm crossover-wheel
.Nd force feedback for the Thrustmaster T150 under CrossOver
.Sh DESCRIPTION
.Nm
gives a Thrustmaster T150 working force feedback in games running under
CrossOver on macOS, without installing anything system wide: no kext, no
DriverKit system extension, no SIP change and no AMFI change.
.Pp
Some of this already works on a stock Mac with nothing installed. macOS
enumerates the wheel as an ordinary joystick once it is in firmware mode, and
CrossOver's HID backend passes the wheel's own report descriptor into the
bottle unchanged, so the steering and pedals reach games already.
.Pp
What is missing is force feedback, because the wheel's descriptor contains no
Physical Interface Device collection and Wine performs no force feedback
translation of its own on macOS. The buttons are missing too, for a different
and undiagnosed reason: the wheel puts all thirteen of them on the wire and
CrossOver registers none.
.Pp
Rather than trying to present a force feedback device to macOS, which is
blocked by restricted entitlements no free path can obtain,
.Nm
presents it inside the bottle. A proxy
.Pa dinput8.dll
wraps DirectInput's force feedback surface and forwards effects to an
unprivileged macOS daemon that owns the wheel through
.Fn IOHIDDeviceSetReport .
.Sh COMPONENTS
.Bl -tag -width "t150-dinput8.dll"
.It Xr t150d 8
The macOS daemon. Owns the wheel, encodes effects, writes output reports.
The protocol, the slot table, the downgrades and the watchdog work; the
backend that would reach a wheel does not exist yet, so it logs instead.
.It Nm t150-dinput8.dll
The in-bottle proxy. Written and cross built, never yet run. It is installed
as
.Pa dinput8.dll
in the bottle's
.Pa system32
and chain-loads CrossOver's builtin from
.Pa dinput8_orig.dll
beside it. It must go there rather than beside a game, because SDL reaches
DirectInput through
.Fn CoCreateInstance ,
which resolves to an absolute
.Pa system32
path.
.It Nm t150boot
One-shot boot to firmware mode switch. Not yet implemented.
.It Nm t150ctl
Rotation range, gain and autocenter from the command line. Not yet
implemented.
.It Xr t150-probe 1
The three Phase 0 measurement tools. Implemented.
.El
.Sh IMPLEMENTATION NOTES
The T150 renders constant force, sine, both sawtooths, spring and damper in
hardware. Square and triangle are not in its protocol despite being
periodics, and neither are friction, inertia and ramp. Refusing those
outright risks a game disabling force feedback altogether, so the daemon
downgrades them instead: square and triangle to sine, friction and inertia to
damper, and ramp to a time-sliced constant.
.Pp
Nothing in this stack learns that a game has exited: Wine's
.Pa hidclass.sys
consumes the close at the physical device object, and DirectInput only sends
a reset on a graceful unacquire. A crashed game would therefore leave the
last commanded force latched on the wheel. The daemon treats silence as a
fault and stops every effect after half a second without traffic.
.Sh STATUS
The probe tools, the shared headers, the wire encoders, the wire protocol,
the daemon's logic and the proxy exist. The macOS HID backend and the command
line tools do not, and the proxy has never been executed.
.Pp
The measurement the project was gated on has been made and it succeeded: an
unprivileged
.Fn IOHIDDeviceSetReport
sets the wheel's autocenter to full and it becomes hard to turn, then
releases it and it turns freely, with CrossOver free to keep reading the
wheel. Force feedback effects are a different matter and still move nothing,
on that pipe or on interrupt OUT, which makes it a question about the packet
layout rather than about macOS. See
.Pa docs/PROBES.md
and RESEARCH.md A19 and A20.
.Pp
Everything written so far is checked by
.Cm make test
on any machine: golden vectors for the encoders, round trips for the
protocol, and the daemon driven through simulated time and over a real
socket. That proves the bytes are the ones
.Pa docs/PROTOCOL.md
describes, and nothing at all about whether the wheel honours them.
.Sh SEE ALSO
.Xr t150-probe 1
.Pp
.Pa docs/HANDOFF.md ,
.Pa docs/RESEARCH.md ,
.Pa docs/ARCHITECTURE.md ,
.Pa docs/PROTOCOL.md ,
.Pa docs/PROBES.md
.Sh AUTHORS
.An Renaud Allard
.Sh CAVEATS
The T150 wire protocol is not published by Thrustmaster. Everything here is
derived from the Linux drivers cited in
.Pa docs/PROTOCOL.md
and from a firmware 3.5 descriptor capture, not from current hardware.
